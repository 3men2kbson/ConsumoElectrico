@model IEnumerable<ConsumoElectrico.Models.LecturaPropia>

@{
    ViewBag.Title = "Index - Lectura Propia";
}

<h2>Index - Lectura Propia</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LPFecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LPLectura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LPSubsidio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LPFecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LPLectura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LPSubsidio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LPId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LPId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LPId })
        </td>
    </tr>
}

</table>
